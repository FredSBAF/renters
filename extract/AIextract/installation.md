# Backend
pip install ollama flask flask-cors pillow pymupdf

# Frontend (dans un autre terminal)
npm create vite@latest frontend -- --template react
cd frontend
npm install lucide-react
npm run dev

